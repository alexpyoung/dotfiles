#!/usr/bin/env bash

set -euo pipefail

main() {
    local -r DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd)"
    pushd "$DIR" > /dev/null
    crontab ../../crontab
    popd >/dev/null
    echo "New crontab installed:"
    crontab -l
}

main

