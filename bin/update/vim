#!/usr/bin/env bash

set -euo pipefail

main() {
    local -r DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd)
    git submodule update --recursive "$DIR"/../../vim/.vim/vim-plug
    git status
}

main
