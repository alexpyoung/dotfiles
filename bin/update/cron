#!/usr/bin/env bash

set -euo pipefail

main() {
    local -r DIR="$(pwd)/$(dirname "${BASH_SOURCE[0]}")/../.."
    local -r FILE=$(python -c "import os; print(os.path.realpath('$DIR/crontab'))")
    echo "Generating crontab..."
    crontab -l > "$FILE"
    git status
}

main
